<!DOCTYPE html>
<head>
    @if(isset($_isMobile) && $_isMobile)
        @includeIf('theme::layout.mobile.head')
    @else
        @includeIf('theme::layout.head')
    @endif
</head>
<body>
<div id="container">
    {{CONTENT}}
</div>
  @if(isset($_isMobile) && $_isMobile)
       @includeIf('theme::layout.mobile.footer')
  @else
       @includeIf('theme::layout.footer')
  @endif

</body>
</html>
